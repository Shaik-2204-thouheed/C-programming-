#include <stdio.h>


void printNumbers(int n) {
    for(int i = 1; i <= n; i++) {
        printf("%d ", i);
    }
}

int main() {
    int n;

    printf("Enter value of n: ");
    scanf("%d", &n);

    printf("Numbers from 1 to %d:\n", n);
    printNumbers(n);

    return 0;
}
